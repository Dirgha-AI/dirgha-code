# Tweet thread draft — dirgha 1.3.0

## Tweet 1 (with video)

dirgha 1.3.0 — a terminal coding agent.

Free compute, full agency. 17 BYOK providers + multi-key cooldown rotation. The free-tier hy3 model wrote most of this release. 38 / 38 offline tests in 16 s.

```
npm i -g @dirgha/code
```

[attach: dirgha-promo MP4]

## Tweet 2

What's new in 1.3.0:

· **Soul** — short Markdown persona at ~/.dirgha/soul.md; ships with a sane default
· **Multi-key BYOK pool** — priority + cooldown rotation; 17 providers known
· **`dirgha update --check / --self / --packages`** — npm registry probe, prompt-gated, audit-logged
· **`dirgha models refresh`** — parallel /v1/models fetch; live: 499 models in <1 s
· **TS / ESM extensions API** — drop a `.mjs` into ~/.dirgha/extensions/<name>/

## Tweet 3

What was broken and is now fixed:

· NVIDIA NIM streaming silently dropped content on deepseek-v4-flash because the parser only recognised `delta.reasoning_content`, not `delta.reasoning`. Both keys now accepted.
· `dirgha keys set` persisted but never hydrated into `process.env`. Fixed.
· Mid-session failover only triggered on turn 0. Now resumes from partial transcripts with remaining-turn budget.
· The `task` subagent tool was implemented but never registered. Wired.

## Tweet 4

Dogfood note:

The 1.3.0 features were specced by Claude and implemented by `dirgha -m hy3` (Tencent Hunyuan-3 preview, free via OR). hy3 nailed sprints 1–3 first-shot — 7/7, 14/14, 14/14 — with one self-corrected `node:test` import.

The promo video itself was authored by hy3 from a brief.

## Tweet 5 (closing)

The matrix file is the contract. Mean 9.82 / 10. Sum-of-gaps 0 across 22 dimensions.

Run `dirgha -m hy3 "your prompt"` and pay nothing.

Repo: github.com/Dirgha-AI/dirgha-code
Issues: welcome (CONTRIBUTING.md inside)

— —

## Notes

- Don't post until: `npm publish 1.3.0` succeeds AND a real second-machine `npm i -g @dirgha/code@1.3.0` smoke confirms the binary works.
- Refresh hyperframes render at `--quality=high` before posting (current is draft).
- Confirm video aspect ratio matches Twitter (16:9 OK, 1:1 better engagement).
- Pin tweet 1 if engagement looks good in first hour.
