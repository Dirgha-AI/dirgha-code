node_modules/
.dirgha/
.fleet/
*.log
.env*
!.env.example
coverage/
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# `dist/` is the shipped build output and IS tracked — the npm tarball
# ships dist/ directly, and downstream installs hit it without a build
# step. Source maps inside dist/ are tracked alongside.
tsconfig.tsbuildinfo
*.tsbuildinfo

# npm pack output — regenerated by verify-install + every `npm pack` run
*.tgz

# Internal authoring artefacts — NEVER ship publicly.
# Canonical rule: docs/PUBLISH-RULES.md in the internal monorepo.
docs/DOCS-CONVENTION.md
docs/PUBLISH-RULES.md
docs/audits/AUDIT-INPUT.md
docs/sessions/
docs/_internal/
promo/MARKETING-BRIEF.md
promo/SCRIPT.md
promo/script-run.log
*-BRIEF.md
*-old-*.md.bak
*.bak

# Promo build artefacts
promo/snapshots/
promo/renders/
