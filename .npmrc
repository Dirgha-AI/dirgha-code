# Temporary shim: marked@17 + marked-terminal@7 peer conflict.
# Sprint 4 of the launch plan resolves this by aligning marked versions;
# at that point this file should be deleted so installs are strict again.
legacy-peer-deps=true
