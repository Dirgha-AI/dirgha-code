--- commands/setup.ts
+++ commands/setup.ts
@@ -2,7 +2,7 @@
 import { Command } from 'commander';
 import chalk from 'chalk';
 import { existsSync, writeFileSync } from 'fs';
-import { safeExec } from '../utils/safe-exec.js';
+import { execCmd } from '../utils/safe-exec.js';
 import { saveCredentials } from '../utils/credentials.js';
 import { loadKeysIntoEnv } from '../utils/keys.js';
 
@@ -81,11 +81,11 @@ export const setupCommand = new Command('setup')
       let opened = false;
       try {
         if (process.platform === 'darwin') {
-          safeExec('open', [signupUrl]);
+          execCmd('open', [signupUrl]);
           opened = true;
         } else if (process.platform === 'win32') {
-          safeExec('cmd.exe', ['/c', 'start', '""', signupUrl]);
+          execCmd('cmd.exe', ['/c', 'start', '""', signupUrl]);
           opened = true;
         } else {
-          safeExec('xdg-open', [signupUrl]);
+          execCmd('xdg-open', [signupUrl]);
           opened = true;
